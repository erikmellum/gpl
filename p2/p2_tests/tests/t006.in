strings[] = {int, double, string, triangle, pixmap, circle,
             rectangle, textbox, forward, initialization, on, animation,
             if, for, else, exit, print, true,
             false, space, leftarrow, rightarrow, uparrow, downarrow,
             leftmousedown, middlemousedown, rightmousedown,
             leftmouseup, middlemouseup, rightmouseup,
             mousemove, mousedrag, akey, skey, dkey, fkey, hkey,
             jkey, kkey, lkey, wkey, sin, cos, tan, asin, acos,
             atan, sqrt, abs, floor, random, touches, near};
